{% extends 'base.html.twig' %}

{% block title %}Student index{% endblock %}

{% block body %}
    <h1>Student index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>IdAfpa</th>
                <th>FirstName</th>
                <th>FamilyName</th>
                <th>Picture</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for student in students %}
            <tr>
                <td class="{% if student.unjustifiedAbsences >= 5 %}text-danger{% endif %}">{{ student.id }}</td>
                <td class="{% if student.unjustifiedAbsences >= 5 %}text-danger{% endif %}">{{ student.idAfpa }}</td>
                <td class="{% if student.unjustifiedAbsences >= 5 %}text-danger{% endif %}">{{ student.firstName }}</td>
                <td class="{% if student.unjustifiedAbsences >= 5 %}text-danger{% endif %}">{{ student.familyName }}</td>
                <td class="{% if student.unjustifiedAbsences >= 5 %}text-danger{% endif %}">{{ student.picture }}</td>
                <td>
                    <a href="{{ path('app_student_show', {'id': student.id}) }}">show</a>
                    {% if is_granted('ROLE_ADMIN') %}<a href="{{ path('app_student_edit', {'id': student.id}) }}">edit</a>{%endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {% if is_granted('ROLE_ADMIN') %}<a href="{{ path('app_student_new') }}" class="btn btn-primary">Create new</a>{%endif %}
    <a href="{{ path('app_home') }}" class="btn btn-outline-secondary">← Retour à l'accueil</a>
{% endblock %}
